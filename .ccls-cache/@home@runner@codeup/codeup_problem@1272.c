// #include<stdio.h>

// int main(void)
// {
//   int k, h; // 입력받을 숫자 선언
//   scanf("%d %d", &k, &h); // 숫자 입력받기

//   // 짝수일 경우 2로 나눈 다음 10배 아니라면 2로 나눠 1을 더한 값
//   k = (k % 2 == 0) ? (k / 2) * 10 : k / 2 + 1;
//   h = (h % 2 == 0) ? (h / 2) * 10 : h / 2 + 1;

//   printf("%d", k + h); // 합을 출력

//   return 0;
// }
